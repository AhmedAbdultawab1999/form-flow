import { UpdateFormCommand } from './update-form-command';

describe('UpdateFormCommand', () => {
  it('should create an instance', () => {
    expect(new UpdateFormCommand()).toBeTruthy();
  });
});
